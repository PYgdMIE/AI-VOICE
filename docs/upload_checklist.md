# Upload Checklist

- Root README matches actual repository paths.
- Model code is under methods/.
- Trained checkpoints are under artifacts/checkpoints/.
- Reports are under artifacts/reports/.
- Full datasets are not committed.
- Missing local-only scripts are either added or removed from docs.
